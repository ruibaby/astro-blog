# Astro Blog

Halo API + Astro = Static Blog 🚀

## 配置

```bash
pnpm install
```

修改 `.env`：

```properties
VITE_URL=http://localhost:8090
VITE_USERNAME=admin
VITE_PASSWORD=admin
```

构建静态页面：

```bash
pnpm build
```

开发环境运行：

```bash
pnpm dev
```
